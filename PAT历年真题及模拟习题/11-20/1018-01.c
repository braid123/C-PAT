#include<stdio.h>
struct{
	char name[5];
	int year;
	int month;
	int day;
};
int main()
{	int N,i;
	int count=0; 
	struct people[10000];
	scanf("%d",&N);
	for(i=0;i<N;i++){
		scanf("%s",&people[i].name);
		scanf("%d/%d/%d",&people[i].year,&people[i].month,&people[i].day);
	}
    if((people[i].year<2014)&&(people[i].year>1814))
    count++;
	else if(people[i].month) 
    return 0;
}
